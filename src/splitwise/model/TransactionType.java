package splitwise.model;

/**
 * Enum representing different types of financial transactions.
 */
public enum TransactionType {
    PAYMENT,    // Direct payment between users
    SETTLEMENT  // Settling all debts between users
}
